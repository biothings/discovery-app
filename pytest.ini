[pytest]
# Disable asyncio_mode to avoid conflicts with Tornado event loops used by biothings
# The biothings framework uses Tornado which manages its own event loops
# Disable pytest-asyncio plugin entirely to avoid event loop conflicts
addopts = -p no:asyncio
