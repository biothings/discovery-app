git+https://github.com/biothings/biothings_schema.py@d79b05c#egg=biothings_schema
# git+https://github.com/biothings/biothings.api.git@0.12.x#egg=biothings

biothings==1.0.1
#PyLD>=0.7.1
#jsonschema>=2.6.0
#torngithub==0.2.0
pycurl>=7.19
python3-saml==1.9.0  # SAML SSO
lxml==4.9.3          # XML parsing (required by python3-saml)
xmlsec==1.3.13       # XML signature support (required by python3-saml)
PyGithub>=1.53
certifi
aiocron==1.8

# pyadf (pyadf==0.2.29) is not on pypi anymore, so we install it from a vendor directory
-e ./vendor/pyadf  # create rich text in Jira

# use in index.routine to lock file
filelock
# s3 backup
boto3>=1.21.0
